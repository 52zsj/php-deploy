---
description: Shell / YAML 同步工具编码约定
globs: "*.sh,data/configs/*.yml,script/**"
alwaysApply: false
---

# Shell 分则（GitShip）

## 技术栈
- 语言：Bash（兼容 macOS bash 3.2，避免关联数组等 bash 4+ 特性）
- 配置：YAML（优先 `yq`，回退 `python3 + PyYAML`）
- 同步：`rsync` + SSH（`sshpass` 可选，密码认证）
- 真相源：Git 管理的文件；不在 Git 中的远程文件不受影响

## 文件职责
- `sync.sh`：主程序（约 2k 行），新功能优先在此按现有函数扩展，勿无故拆文件
- `script/start-ui.sh` + `script/sync-ui/`：本地 Web（配置表单 + Replace 文件管理 + 执行）
  - Replace：env 跟部署组；路径前缀 + 上传文件/文件夹/压缩包；`replace_dir` 默认 `./replace/<配置名>`
- `pack.sh`：根目录快捷打包（默认 macos+windows）→ 实际调用 `script/pack.sh`
- `script/pack.sh`：发行打包 → `dist/`；单文件壳打开即弹配置窗（Chrome/Edge `--app` 或可选 `pywebview`）
  - 产品名：**GitShip**；二进制 / 镜像名：`gitship`
  - 启动流程：解压 → **环境检查** → 缺依赖则引导安装 → 再启动 UI/sync
  - Windows 退出码 **9009** = 找不到命令（多为未装 Python / 商店占位符）；启动器会跳过 `WindowsApps` 伪 python
  - `gitship check`：仅检查依赖
  - 默认安装目录：macOS `~/Library/Application Support/gitship`；Linux `~/.config/gitship`；Windows `%AppData%\gitship`
  - 便携：`GITSHIP_PORTABLE=1`（兼容旧变量 `PHP_DEPLOY_*`）；入口 `script/sync-ui/desktop.py`
- `script/migrate-secrets.sh`：明文密码迁移到 `.secrets/`
- `script/docker-entrypoint.sh` + `Dockerfile` / `docker-compose.yml`：容器部署
  - 镜像名 `gitship`；数据卷 `data/{configs,secrets,credentials,replace,logs,ssh}`
  - 入口命令：`ui`（默认）/ `sync` / `version` / `migrate-secrets` / `bash`
  - 离线构建可 `GITSHIP_BASE_IMAGE=php-deploy:local docker compose build`
  - 离线构建可 `GITSHIP_BASE_IMAGE=php-deploy:local docker compose build`
- `data/configs/demo.yml` / `demo-dir.yml`：仓库模板兼运行时配置目录（与 Docker 共用）
- `data/ssh/`：SSH 私钥（UI 可上传）；`data/repos/`：Git 检出；`data/uploads/`：目录同步工作区
- `data/replace/` / `data/credentials/` / `data/secrets/` / `data/logs/`：其余持久化数据
- `.uploads/<配置名>/`：旧路径兼容（Docker 入口会链到 `data/uploads`）
- `replace/<项目>/<env>/...`：环境配置覆盖源，路径相对仓库根
- `script/`：新增维护 / cron / 一次性 CLI（主入口仍为根目录 `sync.sh`）

## YAML 配置约定（保持字段兼容）
```yaml
gitee:        # 历史键名；repo_url 可指向任意 Git 远端
sync:         # rsync_options, exclude[], replace_dir
default_post_sync_commands: []   # 全局，优先级最低
server_groups:                   # name, env, post_sync_commands, servers[]
  servers[]:  # name, host, target_dir, branch, auth_type, auth_info, post_sync_commands?
```
- `post_sync_commands` 优先级：服务器 > 组 > `default_post_sync_commands`
- 占位符：`{target_dir}` 等，在 `execute_post_sync_commands` 中替换
- 改 YAML schema 属于架构变更，须先确认

## 编码风格
- 函数：`snake_case`（如 `pull_code`、`replace_configs`）
- 全局变量：`UPPER_SNAKE`（如 `CONFIG_FILE`、`LOCAL_DIR`）
- 日志：统一用 `log_info` / `log_success` / `log_warn` / `log_error`，颜色常量已有勿另起一套
- 错误：不用 `set -e`；关键步骤显式检查退出码
- 路径：经 `expand_path` 展开 `~`；用户输入路径经 `validate_path`
- 分支名作变量后缀时用 `sed 's/[^a-zA-Z0-9_]/_/g'`（兼容 bash 3.2）

## 同步策略（勿随意改）
1. Git pull 检测 A/M/D
2. Replace 覆盖（优先级最高）
3. rsync `--checksum` 只传变更；内置排除点开头目录（`.git`/`.cursor`/…）；`sync.exclude` 再过滤上传与删除（`is_excluded`）
4. 删除仅针对 Git 已删文件；命中 exclude 则跳过删除
5. `-f/--force` 强制同步全部 Git 文件（仍应用 exclude）
6. 本地无变更时抽样检查远端文件；缺失则自动全量同步（避免目标被清空后误报成功）
7. `post_sync_commands` 优先级（互斥，只取一层）：服务器节点 > 服务器组 > 全局 default

## 安全
- 密码/凭据：yml 中只用 `secret:KEY` 或 `env:VAR`；明文落在 `.secrets/<配置>.env`（已 gitignore）
- 可用 `./script/migrate-secrets.sh` 把旧明文 yml 批量迁移
- 运行时由 `resolve_secret` 解析；`.credentials/` 仍为 Git HTTPS 临时凭据缓存
- 日志对仓库 URL 脱敏（去掉 user:pass@）
- `.credentials/`、`.secrets/`、含密钥的 yml 明文不得入库
- `replace/` 默认 gitignore 全部内容（仅放本地环境覆盖）
